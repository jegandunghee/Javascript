/* 객체  */
let person = {
  // 키 : 값 > 프로퍼티 
  // html 식으로 보자면 속성 : 속성값이라고 생각하면 될듯 
  user : "홍길동", 
  age : 30, 
  phone : "010-1111-111"
}

console.log(person.user);

person.user = "정재희";
console.log(person.user );